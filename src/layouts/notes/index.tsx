import NoteCard from "@/components/note-card";

const Notes = () => {
  return (
    <section className="mt-12 px-8 grid grid-cols-2 gap-8">
      <NoteCard />
    </section>
  );
};

export default Notes;
