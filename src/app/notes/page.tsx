import Navbar from "@/components/navbar";
import Sidebar from "@/layouts/sidebar";

const NotesPage = () => {
  return (
    <main>
      <Navbar />
      <div>
        <Sidebar />
      </div>
    </main>
  );
};

export default NotesPage;
